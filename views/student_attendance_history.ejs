<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>ประวัติการเช็คชื่อของฉัน</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .report-box{border:2px solid #333;padding:20px;max-width:760px;margin:0 auto}
    .report-title{font-weight:700;text-align:center;margin-bottom:8px}
    .center-line{ text-align:center; margin-bottom:.5rem; }
  </style>
</head>
<body>
  <% if (showNavbar) { %>
    <%- include('partials/navbar', { currentUser, currentRole }) %>
  <% } %>

  <div class="container my-4">
    <div class="report-box">
      <div class="report-title">รายงาน ประวัติการเช็คชื่อเข้าชั้นเรียน</div>
      <!-- ✅ ย้ายชื่อชั้นเรียนมาไว้บรรทัดเดียวใต้หัวรายงาน -->
      <div class="center-line"><strong>ชื่อชั้นเรียน</strong> &nbsp; <%= classroom.classroomname %></div>

      <% if (!hasRecords) { %>
        <div class="alert alert-info text-center my-3">ยังไม่มีประวัติการเช็คชื่อ</div>
      <% } else { %>
        <table class="table table-bordered mb-0">
          <thead class="table-light">
            <tr class="text-center">
              <th style="width:160px;">วันที่</th>
              <th style="width:120px;">เวลา</th>
              <th style="width:220px;">สถานะการเช็คเรียน</th>
            </tr>
          </thead>
          <tbody>
            <% records.forEach(r => { %>
              <tr class="text-center">
                <td><%= r.display_date %></td>
                <td><%= r.display_time %></td>
                <td>
                  <% if (r.status === 'Present') { %>
                    <span class="text-success fw-bold">มาเรียน</span>
                  <% } else if (r.status === 'Late') { %>
                    <span class="text-warning fw-bold">มาสาย</span>
                  <% } else if (r.status === 'Leave') { %>
                    <span class="text-primary fw-bold">ลา</span>
                  <% } else { %>
                    <span class="text-danger fw-bold">ขาดเรียน</span>
                  <% } %>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      <% } %>
    </div>

    <div class="text-center mt-3">
      <a class="btn btn-secondary" href="/classroom/view/<%= classroom.classroomid %>">ย้อนกลับ</a>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
