<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>ประวัติการเช็คชื่อของฉัน</title>
  <link rel="stylesheet" href="/style.css" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    /* เอากรอบออก เหลือแค่ระยะภายในและความกว้าง */
    .report-box { padding: 20px; max-width: 760px; margin: 0 auto; border: 0; }
  </style>
</head>
<body>
  <% if (showNavbar) { %>
    <%- include('partials/navbar', { currentUser, currentRole }) %>
  <% } %>

  <div class="container mt-5 pt-4 mb-5">
    <div class="report-box">
      <h2 class="text-center mb-2">รายงาน ประวัติการเช็คชื่อเข้าชั้นเรียน</h2>
      <div class="text-center text-muted mb-3">
       <h4> ชื่อชั้นเรียน <span class="fw-semibold text-dark"><%= classroom.classroomname %></span></h4>   
      </div>

      <% if (!hasRecords) { %>
        <div class="alert alert-info text-center my-3">ยังไม่มีประวัติการเช็คชื่อ</div>
      <% } else { %>
        <table class="table table-bordered table-hover align-middle mb-0">
          <thead class="table-secondary">
            <tr class="text-center">
              <th style="width:160px;">วันที่</th>
              <th style="width:120px;">เวลา</th>
              <th style="width:220px;">สถานะการเช็คเรียน</th>
            </tr>
          </thead>
          <tbody>
            <% records.forEach(r => { %>
              <tr class="text-center">
                <td><%= r.display_date %></td>
                <td><%= r.display_time %></td>
                <td>
                  <% if (r.status === 'Present') { %>
                    <span class="badge rounded-pill text-bg-success px-3">มาเรียน</span>
                  <% } else if (r.status === 'Late') { %>
                    <span class="badge rounded-pill text-bg-warning px-3">มาสาย</span>
                  <% } else if (r.status === 'Leave') { %>
                    <span class="badge rounded-pill text-bg-primary px-3">ลา</span>
                  <% } else { %>
                    <span class="badge rounded-pill text-bg-danger px-3">ขาดเรียน</span>
                  <% } %>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      <% } %>
    </div>

    <div class="text-center mt-4">
      <a class="btn btn-secondary " href="/classroom/view/<%= classroom.classroomid %>">ย้อนกลับ</a>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
