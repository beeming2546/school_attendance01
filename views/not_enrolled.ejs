<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link rel="stylesheet" href="/style.css" />
  <title>แผงควบคุมผู้ดูแลระบบ</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

  <% if (typeof showNavbar !== 'undefined' && showNavbar && typeof currentUser !== 'undefined') { %>
  <%- include('partials/navbar', { currentUser: currentUser, currentRole: currentRole }) %>
<% } %>
   <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
   <!-- views/not_enrolled.ejs -->
<h4 class="text-danger">คุณไม่ได้อยู่ในชั้นเรียนนี้</h4>
<p>ชั้นเรียน: <strong><%= classroomName %></strong></p>
<p>ผู้ใช้: <strong><%= studentName %></strong></p>
<a href="/classroom" class="btn btn-secondary mt-3">กลับหน้าห้องเรียนของฉัน</a>
</body>
</html>