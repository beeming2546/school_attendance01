<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>เพิ่มผู้เรียนเข้าชั้นเรียน</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="/style.css" />
  <style>
    .required {
      color: red;
      font-weight: bold;
      margin-left: 4px;
    }
    
  /* จัดแถวแบบ 3 คอลัมน์: label | field | *  */
  .line-38{
    display: grid;
    grid-template-columns: 120px 1fr 5px;
    align-items: center;
    margin-bottom: 14px;
  }

  .label-38{
    font-weight: 600;
    color: #606060;
  }

  .field-38 .form-control{
    background: #e9ecef;     /* เทาอ่อนคล้ายภาพ */
  }

  /* ปุ่มให้กึ่งกลางเหมือนหน้าอื่น */
  .btn-group{
    display: flex;
    justify-content: center;
    gap: 15px;
    margin-top: 25px;
  }
  .btn-group .btn{ min-width: 150px; }
  </style>
  <script>
    function validateForm() {
      const ids = document.querySelector('textarea[name="studentIds"]').value.trim();
      const classroomId = document.querySelector('input[name="ClassroomId"]').value;
      if (!ids || !classroomId) {
        alert("กรุณากรอกข้อมูลให้ครบทุกช่อง");
        return false;
      }
      return confirm("คุณต้องการบันทึกข้อมูลนี้ใช่หรือไม่?");
    }
    
  </script>
</head>
<body>
  <% if (showNavbar) { %>
    <%- include('partials/navbar', { currentUser: currentUser, currentRole: currentRole }) %>
  <% } %>

  <div class="container" style="max-width: 600px; margin: 20px auto; padding: 0 15px;">
    <h2 class="mb-4 text-center">เพิ่มผู้เรียนเข้าชั้นเรียน</h2>

    <% if (error) { %>
      <p class="text-danger text-center"><%= error %></p>
    <% } %>

    <form action="/classroom/add-students" method="POST" onsubmit="return validateForm();">
  <div class="panel-38">
    <!-- แถว: รหัสประจำตัว -->
    <div class="line-38">
      <div class="label-38">รหัสประจำตัว:</div>
      <div class="field-38">
        <input type="text" class="form-control"
               name="studentIds"
               placeholder="ใส่หลายคนคั่นด้วย comma เช่น 1001,1002,1003"
               required>
      </div>
      <span class="required">*</span>
    </div>

    <!-- แถว: ชื่อชั้นเรียน -->
    <div class="line-38">
      <div class="label-38">ชื่อชั้นเรียน:</div>
      <div class="field-38">
        <input type="text" class="form-control" value="<%= classroomName %>" readonly>
        <input type="hidden" name="ClassroomId" value="<%= classroomId %>">
      </div>
      <span class="required">*</span>
    </div>
  </div>

  <center>
    <button type="submit" class="btn btn-primary">บันทึก</button>
    <a href="/classroom/<%= classroomId %>/students" class="btn btn-secondary">ย้อนกลับ</a>
  </center>
</form>
 
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
