<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>เลือกวันที่เพื่อสร้าง QR</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="/style.css" />
  <style>
    .required { color: red; font-weight: bold; margin-left: 0px; white-space: nowrap; }

    /* 2 คอลัมน์ตลอด: label | field */
    .form-row{
      display: grid;
      grid-template-columns: 140px 1fr;
      column-gap: 12px;
      row-gap: 12px;
      align-items: center;
    }
    .form-row label{
      font-weight: 500;
      margin: 0;
      justify-self: end;     /* label ชิดขวา */
      white-space: nowrap;
    }

    /* input + * ต่อกันในคอลัมน์เดียว */
    .field{
      display: inline-flex;
      align-items: center;
      width: 100%;
      min-width: 0;
      gap: 8px;
    }
    .field .form-control{
      flex: 1 1 auto;
      min-width: 0;
    }

    /* บรรทัดอธิบายใต้ช่องให้จัดเข้ากริดเดียวกัน */
    .help-col{
      display: grid;
      grid-template-columns: 140px 1fr;
      column-gap: 12px;
      align-items: start;
    }
    .help-col .spacer{ /* ช่องว่างแทน label */
      height: 0;
    }

    @media (max-width: 768px){
      .form-row{ grid-template-columns: 120px 1fr; }
      .help-col{ grid-template-columns: 120px 1fr; }
    }
    @media (max-width: 480px){
      .form-row{ grid-template-columns: 100px 1fr; }
      .help-col{ grid-template-columns: 100px 1fr; }
    }
  </style>
</head>
<body>
  <% if (showNavbar) { %>
    <%- include('partials/navbar', { currentUser, currentRole }) %>
  <% } %>

  <div class="container" style="max-width: 600px; margin: 20px auto; padding: 0 15px;">
    <h2 class="mb-4 text-center">ข้อมูลเพื่อสร้าง QR Code</h2>

    <form action="/classroom/<%= classroomId %>/generate-token" method="POST">
      <!-- ชื่อชั้นเรียน -->
      <div class="form-row">
        <label for="classroomName">ชื่อชั้นเรียน:</label>
        <div class="field">
          <input type="text" id="classroomName" class="form-control" value="<%= classroomName %>" readonly>
          <span class="required">*</span>
        </div>
      </div>

      <!-- วันที่ -->
      <div class="form-row">
        <label for="date">วันที่:</label>
        <div class="field">
          <input
            type="date"
            class="form-control"
            name="date"
            id="date"
            required
            value="<%= new Date().toISOString().split('T')[0] %>"
            readonly
          >
          <span class="required">*</span>
        </div>
      </div>

      <!-- ✅ ถือว่าสายเมื่อเกิน (นาที) -->
      <div class="form-row">
        <label for="grace_minutes">ถือว่าสายเมื่อเกิน:</label>
        <div class="field">
          <div class="input-group w-auto">
            <input
              type="number"
              class="form-control"
              name="grace_minutes"
              id="grace_minutes"
              value="<%= typeof graceMinutes !== 'undefined' ? graceMinutes : 20 %>"
              min="0"
              step="5"
              required
            >
            <span class="input-group-text">นาที</span>
          </div>
          <span class="required">*</span>
        </div>
      </div>
      <!-- บรรทัดคำอธิบายใต้ช่อง -->
      <div class="help-col mb-1">
        <div class="spacer"></div>
        <small class="text-muted">
          เช่น ใส่ <strong>20</strong> หมายถึง “หลังเวลาเริ่มเรียน + 20 นาที” จะถูกนับเป็น <strong>มาสาย</strong>
        </small>
      </div>

      <div class="text-center mt-3">
        <button type="submit" class="btn btn-primary">ยืนยันและสร้าง QR Code</button>
        <a href="/classroom/view/<%= classroomId %>" class="btn btn-secondary ms-2">ย้อนกลับ</a>
      </div>
    </form>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
