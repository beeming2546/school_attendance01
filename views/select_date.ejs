<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>เลือกวันที่เพื่อสร้าง QR</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="/style.css" />
  <style>
    .required { color: red; font-weight: bold; margin-left: 0px; white-space: nowrap; }

/* 2 คอลัมน์ตลอด: label | field */
.form-row{
  display: grid;
  grid-template-columns: 140px 1fr;   /* ปรับตามความยาว label ของคุณ */
  column-gap: 12px;
  row-gap: 12px;
  align-items: center;
  margin-bottom: 2;
}
.form-row label{
  font-weight: 500;
  margin: 0;
  justify-self: end;                  /* label ชิดขวา */
  white-space: nowrap;                /* ไม่ให้ label หักบรรทัด */
}

/* input + * ต่อกันในคอลัมน์เดียว */
.field{
  display: inline-flex;
  align-items: center;
  width: 100%;
  min-width: 0;                       /* กัน input ดันบรรทัดแตก */
  gap: 8px;
}
.field .form-control{
  flex: 1 1 auto;
  min-width: 0;                       /* ให้หดได้เมื่อจอแคบ */
}

/* จอเล็ก: ยังเป็น 2 คอลัมน์ แต่ย่อความกว้างคอลัมน์ label ลง */
@media (max-width: 768px){
  .form-row{ grid-template-columns: 120px 1fr; }
}
@media (max-width: 480px){
  .form-row{ grid-template-columns: 100px 1fr; }
}
  </style>
</head>
<body>
  <% if (showNavbar) { %>
    <%- include('partials/navbar', { currentUser, currentRole }) %>
  <% } %>

  <div class="container" style="max-width: 600px; margin: 20px auto; padding: 0 15px;">
    <h2 class="mb-4 text-center">เลือกวันที่เพื่อสร้าง QR Code</h2>

    <form action="/classroom/<%= classroomId %>/generate-token" method="POST">
      <div class="form-row">
  <label for="classroomName">ชื่อชั้นเรียน:</label>
  <div class="field">
    <input type="text" id="classroomName" class="form-control" value="<%= classroomName %>" readonly>
    <span class="required">*</span>
  </div>
</div>

<div class="form-row">
  <label for="date">วันที่:</label>
  <div class="field">
    <input type="date" class="form-control" name="date" id="date" required
           value="<%= new Date().toISOString().split('T')[0] %>">
    <span class="required">*</span>
  </div>
</div>

      <div class="text-center mt-3">
        <button type="submit" class="btn btn-primary">ยืนยันและสร้าง QR Code</button>
        <a href="/classroom/view/<%= classroomId %>" class="btn btn-secondary ms-2">ย้อนกลับ</a>
      </div>
    </form>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
