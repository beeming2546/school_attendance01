<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>ดูรายละเอียดห้องเรียน</title>
  <link rel="stylesheet" href="/style.css" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body>
  <% if (showNavbar) { %>
    <%- include('partials/navbar', { currentUser: currentUser, currentRole: currentRole }) %>
  <% } %>

  <div class="container mt-4">
    <h2 class="text-center">รายละเอียดห้องเรียน</h2>
    <table class="table table-bordered mt-4">
      <tr>
        <th>ชื่อห้องเรียน</th>
        <td><%= classroom.classroomname %></td>
      </tr>
      <tr>
        <th>เลขห้อง</th>
        <td><%= classroom.roomnumber %></td>
      </tr>
      <tr>
        <th>คำอธิบาย</th>
        <td><%= classroom.description %></td>
      </tr>
      <tr>
        <th>เปอร์เซ็นต์เข้าเรียนขั้นต่ำ</th>
        <td><%= classroom.minattendancepercent %> %</td>
      </tr>
      <tr>
        <th>อาจารย์</th>
        <td><%= classroom.teacher_fullname %></td>
      </tr>
    </table>
    <div class="text-center mt-3">
  <div class="text-center mt-3">
  <% if (currentRole === 'teacher') { %>
    <div class="d-grid gap-2 d-sm-flex justify-content-sm-center mt-1">
    <a href="/classroom/<%= classroom.classroomid %>/students" class="btn btn-primary ">ดูรายชื่อผู้เรียน</a>
    <a href="/classroom/<%= classroom.classroomid %>/select-date" class="btn btn-success ">สร้าง QR Code เช็กชื่อ</a>
    <a href="/classroom/<%= classroom.classroomid %>/attendance-scores" class="btn btn-primary ">ดูคะแนนเช็คชื่อ (ทั้งห้อง)</a>
    <a href="/classroom/<%= classroom.classroomid %>/history" class="btn btn-primary ">ดูประวัติเช็คชื่อ (ทั้งห้อง)</a>
  </div>
    <% } %>
  
</div>




<% if (currentRole === 'student') { %>
  <div class="mt-3">
   
    <div class="d-grid gap-2 d-sm-flex justify-content-sm-center mt-1">
  <a href="/student/classroom/<%= classroom.classroomid %>/attendance-score"
     class="btn btn-primary">
    ดูคะแนนการเช็คชื่อของฉัน
  </a>

  <a href="/student/classroom/<%= classroom.classroomid %>/attendance-history"
     class="btn btn-primary">
    ดูประวัติการเช็คชื่อของฉัน
  </a>
</div>

  </div>
<% } %>

<div class="text-center mt-3">
<a href="/classroom" class="btn btn-secondary">ย้อนกลับ</a>
</div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>