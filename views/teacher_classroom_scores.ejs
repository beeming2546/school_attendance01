<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>คะแนนการเช็คชื่อของผู้เรียน (ทั้งห้อง)</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .report-box { border:1px solid #333; padding:18px; }
    .report-title { font-weight:700; text-align:center; margin-bottom:10px; }
    .label { font-weight:600; }
  </style>
</head>
<body>
  <% if (showNavbar) { %>
    <%- include('partials/navbar', { currentUser, currentRole }) %>
  <% } %>

  <div class="container my-4">
    <div class="report-box">
      <div class="report-title">รายงาน ข้อมูลคะแนนการเช็คชื่อเข้าชั้นเรียนของผู้เรียน</div>

      <p class="mb-1 text-center">
        <span class="label">ชื่อชั้นเรียน</span> &nbsp; <%= classroom.classroomname %>
      </p>
      <p class="mb-3 text-center">
        <span class="label">เปอร์เซ็นต์การเข้าเรียนขั้นต่ำ</span> &nbsp; <%= minPercent %>%
      </p>

      <% if (!hasAnySession) { %>
        <div class="alert alert-info text-center my-3">
          ยังไม่มีการเช็คชื่อของชั้นเรียนนี้
        </div>
      <% } %>

      <table class="table table-bordered mt-2">
        <thead class="table-light">
          <tr class="text-center">
            <th style="width:140px;">รหัสประจำตัว</th>
            <th>ชื่อ</th>
            <th>นามสกุล</th>
            <th style="width:140px;">จำนวนเข้าเรียน</th>
            <th style="width:140px;">จำนวนขาดเรียน</th>
            <th style="width:160px;">เปอร์เซ็นต์การเข้าเรียน</th>
            <th style="width:180px;">สถานะการเข้าเรียน</th>
          </tr>
        </thead>
        <tbody>
          <% scores.forEach(s => { %>
            <tr class="align-middle text-center">
              <td><%= s.studentid %></td>
              <td class="text-start"><%= s.firstname %></td>
              <td class="text-start"><%= s.surname %></td>
              <td><%= s.present %></td>
              <td><%= s.absent %></td>
              <td><%= s.percent %>%</td>
              <td>
                <% if (s.isPass) { %>
                  <span class="text-success fw-bold">ผ่านเกณฑ์</span>
                <% } else { %>
                  <span class="text-danger fw-bold">ไม่ผ่านเกณฑ์</span>
                <% } %>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>

      <div class="text-end">
        <small class="text-muted">คาบทั้งหมดในห้องนี้: <%= totalSessions %> คาบ</small>
      </div>
    </div>

    <div class="mt-3">
      <a href="/classroom/<%= classroom.classroomid %>/students" class="btn btn-secondary">ย้อนกลับ</a>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
