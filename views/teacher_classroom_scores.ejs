<!DOCTYPE html> 
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>คะแนนการเช็คชื่อของผู้เรียน (ทั้งห้อง)</title>
  <link rel="stylesheet" href="/style.css" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <% if (showNavbar) { %>
    <%- include('partials/navbar', { currentUser, currentRole }) %>
  <% } %>

  <div class="container my-5">
    <h2 class="text-center mb-4">รายงาน ข้อมูลคะแนนการเช็คชื่อเข้าชั้นเรียนของผู้เรียน</h2>

    <div class="text-center mb-2">
      <div class="fs-5 fw-semibold mb-1">
        ชื่อชั้นเรียน <span class="text-dark"><%= classroom.classroomname %></span>
      </div>
      <div class="fs-6">
        เปอร์เซ็นต์การเข้าเรียนขั้นต่ำ
        <span class="fw-bold text-dark"><%= minPercent %>%</span>
      </div>
    </div>

    <!-- ✅ อธิบายกติกา -->
    <div class="text-center mb-3">
  <small class="text-muted">
    เกณฑ์: <strong>มาสาย 3 ครั้ง = ขาด 1 ครั้ง </strong>
    (เปอร์เซ็นต์คิดจากค่าหลังคิดโทษ)
  </small>
</div>
  

    <% if (!hasAnySession) { %>
      <div class="alert alert-info text-center my-3">
        ยังไม่มีการเช็คชื่อของชั้นเรียนนี้
      </div>
    <% } %>

    <div class="table-responsive mt-3">
      <table class="table table-bordered mt-2 align-middle">
        <thead class="table-secondary">
          <tr class="text-center">
            <th style="width:140px;">รหัสประจำตัว</th>
            <th>ชื่อ</th>
            <th>นามสกุล</th>
            <th style="width:140px;">จำนวนมาตรงเวลา</th>
            <th style="width:140px;">จำนวนมาสาย</th>
            <th style="width:160px;">จำนวนเข้าเรียน (รวม)</th>
            <th style="width:140px;">จำนวนขาดเรียน</th>
            <th style="width:160px;">เปอร์เซ็นต์การเข้าเรียน</th>
            <th style="width:180px;">สถานะการเข้าเรียน</th>
          </tr>
        </thead>
        <tbody>
          <% scores.forEach(s => { %>
            <tr class="text-center">
              <td><%= s.studentid %></td>
              <td class="text-start"><%= s.firstname %></td>
              <td class="text-start"><%= s.surname %></td>

              <td><%= s.ontime %></td>
              <td><%= s.late %></td>
              <td><%= s.present %></td>

              <!-- ✅ ขาดเรียน (แสดง tooltip ถ้ามีโทษจากสาย) -->
              <td>
  <%= s.absent %>
  <% if (s._late_penalty && s._late_penalty > 0) { %>
    <span class="text-muted"
          title="ขาดจริง: <%= s._absent_raw %> | โทษจากสาย: <%= s._late_penalty %>">
      (รวมโทษสาย)
    </span>
  <% } %>
</td>


              <td><%= s.percent %>%</td>
              <td class="text-center">
                <% if (s.isPass) { %>
                  <span class="badge rounded-pill text-bg-success px-3 py-2">ผ่านเกณฑ์</span>
                <% } else { %>
                  <span class="badge rounded-pill text-bg-danger px-3 py-2">ไม่ผ่านเกณฑ์</span>
                <% } %>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>

    <div class="d-grid gap-2 d-sm-flex justify-content-sm-center mt-1">
      <span class="badge rounded-pill text-bg-primary px-3 py-2 fs-6">
        คาบทั้งหมดในห้องนี้: <%= totalSessions %> คาบ
      </span>
    </div>

    <div class="mt-3">
      <center><a href="/classroom/view/<%= classroom.classroomid %>" class="btn btn-secondary">ย้อนกลับ</a></center>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
