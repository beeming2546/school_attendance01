<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>ยืนยันการเช็คชื่อ</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .box { max-width: 520px; margin: 40px auto; border: 2px solid #333; padding: 24px; }
    .req { color: #dc3545; font-weight: 700; margin-left: 6px; }
  </style>
</head>
<body>
  <% if (showNavbar) { %>
    <%- include('partials/navbar', { currentUser, currentRole }) %>
  <% } %>

  <div class="box">
    <h5 class="text-center mb-4">หน้ายืนยันบันทึกการเช็คชื่อ</h5>
    <h5 class="text-center mb-4">โปรดรีบยืนยันให้แล้วเสร็จ เนื่องจากเวลาของ qrcode ยังนับอยู่</h5>

    <form action="/attendance/confirm" method="POST" class="mx-auto" style="max-width:420px">
  <div class="mb-3">
    <label class="form-label">ชื่อชั้นเรียน</label>
    <input type="text" class="form-control" value="<%= classroomName %>" readonly>
  </div>
  <div class="mb-3">
    <label class="form-label">วันที่</label>
    <input type="text" class="form-control" value="<%= date %>" readonly>
  </div>
  <div class="mb-3">
    <label class="form-label">เวลา</label>
    <input type="text" class="form-control" value="<%= time %>" readonly>
  </div>
  <div class="mb-3">
    <label class="form-label">รหัสประจำตัว</label>
    <input type="text" class="form-control" value="<%= studentId %>" readonly>
  </div>

  <input type="hidden" name="token" value="<%= token %>">

  <button type="submit" class="btn btn-primary w-100">ยืนยันการเช็คชื่อ</button>
</form>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
  
</body>
</html>
