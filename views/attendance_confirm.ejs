<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>ยืนยันการเช็คชื่อ</title>
  <link rel="stylesheet" href="/style.css" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    /* กรอบดำ */
    .box{
      max-width: 600px;
      margin: 20px auto;
      border: 2px solid #333;
      padding: 24px;
    }

    .required{ color: red; font-weight: bold; margin-left: 0; white-space: nowrap; }

    /* 2 คอลัมน์ตลอด: label | field */
    .form-row{
      display: grid;
      grid-template-columns: 140px 1fr;
      column-gap: 12px;
      row-gap: 12px;
      align-items: center;
    }
    .form-row label{
      font-weight: 500;
      margin: 0;
      justify-self: end;         /* label ชิดขวา */
      white-space: nowrap;
    }

    /* input + * ต่อกันในคอลัมน์เดียว */
    .field{
      display: inline-flex;
      align-items: center;
      width: 100%;
      min-width: 0;
      gap: 8px;
    }
    .field .form-control{
      flex: 1 1 auto;
      min-width: 0;
    }

    /* Responsive ให้เหมือนไฟล์ select_date.ejs */
    @media (max-width: 768px){
      .form-row{ grid-template-columns: 120px 1fr; }
    }
    @media (max-width: 480px){
      .form-row{ grid-template-columns: 100px 1fr; }
    }
  </style>
</head>
<body>
  <% if (showNavbar) { %>
    <%- include('partials/navbar', { currentUser, currentRole }) %>
  <% } %>

  <div class="box">
    <h5 class="text-center mb-2">หน้ายืนยันบันทึกการเช็คชื่อ</h5>
    <h6 class="text-center mb-4">โปรดรีบยืนยันให้แล้วเสร็จ เนื่องจากเวลาของ qrcode ยังนับอยู่</h6>

    <form action="/attendance/confirm" method="POST">
      <div class="form-row">
        <label>ชื่อชั้นเรียน:</label>
        <div class="field">
          <input type="text" class="form-control" value="<%= classroomName %>" readonly>
          <span class="required">*</span>
        </div>
      </div>

      <div class="form-row">
        <label>วันที่:</label>
        <div class="field">
          <input type="text" class="form-control" value="<%= date %>" readonly>
          <span class="required">*</span>
        </div>
      </div>

      <div class="form-row">
        <label>เวลา:</label>
        <div class="field">
          <input type="text" class="form-control" value="<%= time %>" readonly>
          <span class="required">*</span>
        </div>
      </div>

      <div class="form-row">
        <label>รหัสประจำตัว:</label>
        <div class="field">
          <input type="text" class="form-control" value="<%= studentId %>" readonly>
          <span class="required">*</span>
        </div>
      </div>

      <input type="hidden" name="token" value="<%= token %>">

      <div class="text-center mt-3">
        <button type="submit" class="btn btn-primary">ยืนยันการเช็คชื่อ</button>
      </div>
    </form>
  </div>
  <center><a href="/scan" class="btn btn-secondary">ย้อนกลับ</a></center>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
